name: release-readiness-score

on:
  workflow_dispatch:
  push:
    paths:
      - ecosystem.json
      - scripts/generate-release-readiness-score.py

jobs:
  release-readiness:
    uses: Jonathhhan/ofxGgmlWorkflows/.github/workflows/release-readiness-score.yml@main
