name: baseline-compatibility

on:
  workflow_dispatch:
  push:
    paths:
      - ecosystem.json
      - "**/ofxggml-addon.json"
      - scripts/check-baseline-compatibility.py

jobs:
  baseline-compatibility:
    uses: Jonathhhan/ofxGgmlWorkflows/.github/workflows/baseline-compatibility.yml@main
