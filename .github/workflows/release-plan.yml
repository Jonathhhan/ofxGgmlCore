name: release-plan

on:
  workflow_dispatch:
  push:
    paths:
      - ecosystem.json
      - scripts/plan-release.py

jobs:
  release-plan:
    uses: Jonathhhan/ofxGgmlWorkflows/.github/workflows/release-plan.yml@main
