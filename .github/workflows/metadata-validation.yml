name: metadata-validation

on:
  workflow_dispatch:
  push:
    paths:
      - "**/ofxggml-addon.json"
      - scripts/validate-addon-metadata.py

jobs:
  metadata-validation:
    uses: Jonathhhan/ofxGgmlWorkflows/.github/workflows/metadata-validation.yml@main
